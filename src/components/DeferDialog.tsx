import {useState,type FormEvent} from 'react'; import type {Task} from '../types'; import {deferTask} from '../lib/repository'; import type {DeferDecision} from '../lib/rollover';
export function DeferDialog({task,onClose}:{task:Task;onClose:()=>void}){const [decision,setDecision]=useState<DeferDecision>('reschedule');const [next,setNext]=useState(task.nextActionText);const [date,setDate]=useState('');const [error,setError]=useState('');const intervention=task.deferCount>=1;async function submit(e:FormEvent){e.preventDefault();try{await deferTask(task,decision,next,date||undefined);onClose()}catch(err){setError(err instanceof Error?err.message:'Please choose what happens next.')}}return <div className="fixed inset-0 z-50 grid place-items-end bg-ink/35 p-0 sm:place-items-center sm:p-4" role="dialog" aria-modal="true" aria-labelledby="replan-title"><form onSubmit={submit} className="w-full max-w-lg rounded-t-3xl bg-cream p-6 shadow-soft sm:rounded-3xl"><h2 id="replan-title" className="text-xl font-bold">{intervention?'What makes this hard to start?':'Choose what happens next'}</h2><p className="mt-1 text-sm text-slate-600">No automatic rollover. Make a deliberate, kind choice.</p><div className="mt-5 grid grid-cols-2 gap-2">{(['reschedule','shrink','waiting','delegate','drop','someday'] as DeferDecision[]).map(x=><button type="button" key={x} onClick={()=>setDecision(x)} className={`min-h-12 rounded-xl border px-3 capitalize ${decision===x?'border-sage bg-sage text-white':'border-sand bg-white'}`}>{x}</button>)}</div>{decision==='reschedule'&&<input aria-label="New date" type="date" value={date} onChange={e=>setDate(e.target.value)} className="mt-4 min-h-12 w-full rounded-xl border border-sand bg-white px-3"/>}{(decision==='shrink'||intervention)&&<><label className="mt-4 block text-sm font-semibold">Smaller physical next action</label><input value={next} onChange={e=>setNext(e.target.value)} placeholder="Open… / Email… / Write…" className="mt-1 min-h-12 w-full rounded-xl border border-sand bg-white px-3"/></>}{error&&<p role="alert" className="mt-3 text-sm text-clay">{error}</p>}<div className="mt-6 flex justify-end gap-2"><button type="button" onClick={onClose} className="min-h-12 px-4">Cancel</button><button className="min-h-12 rounded-xl bg-sage px-5 font-semibold text-white">Confirm plan</button></div></form></div>}
