import {useState} from 'react';import {createProject} from '../lib/repository';import {useProjects} from '../hooks/useData';
export function Plan(){return <><h1 className="text-3xl font-bold">Seven-day plan</h1><p className="mt-3 text-slate-600">A capacity view for intentions—not a packed calendar.</p><div className="mt-8 grid gap-3 sm:grid-cols-2 lg:grid-cols-3">{Array.from({length:7},(_,i)=>{const d=new Date();d.setDate(d.getDate()+i);return <section key={i} className="min-h-36 rounded-2xl border border-sand bg-white p-4"><h2 className="font-bold">{new Intl.DateTimeFormat(undefined,{weekday:'short',month:'short',day:'numeric'}).format(d)}</h2><p className="mt-2 text-sm text-slate-500">Keep room for reality.</p></section>})}</div></>}
export function Projects(){const projects=useProjects(),[title,setTitle]=useState('');return <><h1 className="text-3xl font-bold">Projects</h1><form className="mt-6 flex gap-2" onSubmit={async e=>{e.preventDefault();if(title.trim()){await createProject(title);setTitle('')}}}><input value={title} onChange={e=>setTitle(e.target.value)} placeholder="New project outcome" className="min-h-12 flex-1 rounded-xl border border-sand bg-white px-4"/><button className="rounded-xl bg-sage px-5 font-semibold text-white">Add</button></form><div className="mt-8 space-y-3">{projects.map(p=><article key={p.id} className="rounded-2xl border border-sand bg-white p-5"><h2 className="font-bold">{p.title}</h2><p className="mt-1 text-sm text-slate-500">Needs a definition of done and next action</p></article>)}</div></>}
